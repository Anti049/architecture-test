name: architecture_test_workspace
publish_to: none

environment:
  sdk: ^3.6.0

# WORKSPACE 1 (main / latest stack).
# Managed by Melos in this root; the Isar apps live in ./isar-workspace with
# their OWN isolated workspace root so incompatible codegen stacks never
# resolve together.

dev_dependencies:
  flutter_lints: ^5.0.0
  melos: ^6.3.0
